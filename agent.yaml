# =====================================================
# 🧠 agent.yaml — Configuración del Agente IA
# Proyecto: SL2000-Next
# Autor: BeLc3bU
# Descripción:
#   Archivo de configuración para cualquier IA que
#   colabore en el desarrollo del sistema SL2000-Next.
# =====================================================

context: |
  SL2000-Next es una reconstrucción moderna del sistema SL2000
  utilizado por el Ejército del Aire y del Espacio (España).
  Su objetivo es crear una plataforma full-stack modular para
  la gestión logística, mantenimiento, operaciones y recursos
  de aeronaves y vehículos, con énfasis en seguridad, trazabilidad
  y automatización.

  El proyecto debe funcionar en entornos Docker, con backend en
  Node.js (Express), frontend en React (Vite), y base de datos
  PostgreSQL. El agente IA colaborará en la generación, documentación
  y optimización del código, manteniendo coherencia entre módulos
  y promoviendo buenas prácticas de ingeniería.

objectives:
  - Diseñar y mantener una arquitectura escalable, limpia y segura.
  - Asegurar coherencia entre backend, frontend y base de datos.
  - Implementar autenticación JWT con roles definidos.
  - Documentar automáticamente rutas, controladores y modelos.
  - Automatizar tareas repetitivas sin comprometer seguridad.
  - Proponer mejoras de rendimiento y mantenibilidad.
  - Preservar la filosofía operativa del sistema SL2000 original.

guidelines:
  - Seguir principios SOLID, DRY y KISS.
  - Validar y sanitizar todos los datos de entrada/salida.
  - Mantener el código modular y comentado.
  - Evitar dependencias innecesarias.
  - Priorizar la seguridad y la estabilidad sobre la velocidad.
  - Explicar siempre el propósito del código antes de generarlo.
  - No generar ni usar información sensible o clasificada.
  - Toda comunicación debe ser técnica, profesional y precisa.

stack:
  backend:
    - Node.js (Express)
    - PostgreSQL (Sequelize ORM)
    - JWT (Autenticación)
    - Jest (Testing)
  frontend:
    - React + Vite
    - Axios (API requests)
    - Zustand o Redux (estado global)
    - TailwindCSS (UI)
  infra:
    - Docker + Docker Compose
    - GitHub Actions (CI/CD opcional)
    - ESLint + Prettier (estilo de código)

roles:
  - Administrador: control total del sistema (CRUD completo)
  - Técnico: registro y actualización de mantenimiento
  - Operador: acceso a informes y operaciones limitadas
  - Invitado: lectura restringida de datos

output_format:
  - Todas las respuestas deben estructurarse así:
    1. 🧩 **Resumen técnico**
    2. 🧠 **Explicación del enfoque**
    3. 💻 **Código o comandos**
    4. 🧪 **Ejemplo o validación**
  - Escribir el código en inglés, los comentarios y explicaciones en español.
  - Mantener un formato limpio y legible (Markdown + bloques de código).
  - Si hay varias soluciones posibles, explicar ventajas y desventajas.

naming_conventions:
  files: "kebab-case"
  variables: "camelCase"
  components: "PascalCase"
  database_tables: "snake_case"

security:
  - Nunca incluir claves, tokens o datos reales.
  - No exponer configuraciones críticas en el código generado.
  - Todas las rutas y acciones deben tener validación de permisos.
  - Cifrar contraseñas con bcrypt o equivalente.
  - Usar HTTPS y CORS configurado correctamente.

collaboration:
  - Las IAs deben coordinar sus cambios respetando ramas y commits:
    - feature/<nombre_modulo>
    - fix/<nombre_bug>
  - Los mensajes de commit seguirán el formato Conventional Commits.
  - Se deben generar logs y documentación automática de los endpoints.

license: MIT
version: 1.0.0
status: "Pre-Alpha"
